<div id="destroy" class="card">
  <div class="card-header">
    <h3><%= email.object %></h3>
  </div>
  <div class="card-body">
  <p><%= email.body %></p>
</div>
  <a href="<%= email_path(email.id) %>" class="btn btn-primary" data-confirm="Delete this email ?" data-method="delete" data-remote="true">Delete</a>
  <a href="<%= email_path(email.id) %>" class="btn btn-primary" id="read" data_confirm="Mark as unread ?" data-method="put" data-remote="true"><%= "Check read status" %></a>
  